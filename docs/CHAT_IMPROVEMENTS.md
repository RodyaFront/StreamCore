# Улучшения виджета чата

## Обзор

Этот документ описывает улучшения **визуального виджета чата для OBS**. Виджет предназначен только для отображения сообщений на стриме, без интерактивных элементов.

**Особенности виджета**:
- Отображает последние 100 сообщений
- Сообщения автоматически удаляются через 1 минуту
- Чат всегда показывает последние сообщения (автопрокрутка вниз)
- Нет интерактивности - только визуальное отображение

## 🎯 Реализуемые улучшения

### 1. Подсветка упоминаний (@username)

**Проблема**: Упоминания в сообщениях не выделяются визуально.

**Решение**:
- Подсветка упоминаний в тексте сообщения
- Визуальное выделение (цвет, жирный шрифт)
- Работает автоматически при парсинге сообщения

**Реализация**:
- Добавление функции `parseMentions` в `EmoteService` или создание отдельного сервиса форматирования
- Регулярное выражение для поиска упоминаний: `/@(\w+)/g`
- Замена на HTML с классом `mention` для стилизации

### 2. Обработка ссылок

**Проблема**: Длинные ссылки могут выходить за границы сообщения и не форматируются.

**Решение**:
- Автоматическое определение и форматирование ссылок
- Сокращение очень длинных ссылок (более 50 символов)
- Визуальное выделение ссылок
- Автоматический перенос длинных слов

**Реализация**:
- Добавление функции `formatLinks` в сервис форматирования
- Регулярное выражение для поиска ссылок: `/(https?:\/\/[^\s]+)/g`
- Замена на HTML с классом `message-link` для стилизации
- Сокращение длинных URL

## 📝 Примечания

- Все улучшения работают автоматически при парсинге сообщения
- Форматирование происходит на backend перед отправкой на frontend
- Стили для упоминаний и ссылок добавляются в компонент ChatMessage
- Важно сохранить текущий визуальный стиль (STALKER тематика)
- Изменения должны быть обратно совместимыми
