# Аудит Streak-системы

## Обзор

Аудит реализованной streak-системы с точки зрения продакшн-готовности, принципов SOLID, KISS, DRY и лучших практик.

## 🔴 Критические проблемы

### 1. DRY нарушение: Дублирование логики расчета streak

**Проблема**: Методы `getCurrentStreak` и `getStreakForStream` содержат идентичную логику расчета streak.

**Файл**: `src/backend/services/bonuses/StreakService.js:85-177`

**Решение**: Вынести общую логику в отдельный приватный метод.

```javascript
// Плохо (текущий код)
async getCurrentStreak(username) {
    // ... 40+ строк дублированного кода
}

async getStreakForStream(username, streamDate) {
    // ... 40+ строк почти идентичного кода
}

// Хорошо
_calculateStreakFromVisits(visits, targetDate) {
    // Общая логика
}

async getCurrentStreak(username) {
    const visits = await this._getVisits(username);
    const today = new Date();
    return this._calculateStreakFromVisits(visits, today);
}

async getStreakForStream(username, streamDate) {
    const visits = await this._getVisitsForDate(username, streamDate);
    return this._calculateStreakFromVisits(visits, new Date(streamDate));
}
```

### 2. Производительность: Prepared statements создаются каждый раз

**Проблема**: В методах `markVisit`, `getCurrentStreak`, `getStreakForStream` prepared statements создаются при каждом вызове.

**Файл**: `src/backend/services/bonuses/StreakService.js:64, 93, 140`

**Решение**: Создавать prepared statements один раз в конструкторе или использовать паттерн из `queries/`.

```javascript
// Плохо (текущий код)
async markVisit(username) {
    const insertVisit = db.prepare(`...`); // Создается каждый раз
    const result = insertVisit.run(...);
}

// Хорошо
class StreakService {
    constructor() {
        this.insertVisit = db.prepare(`
            INSERT INTO stream_visits (username, stream_date, first_message_at)
            VALUES (?, ?, CURRENT_TIMESTAMP)
            ON CONFLICT(username, stream_date) DO NOTHING
        `);
        this.getVisits = db.prepare(`
            SELECT stream_date
            FROM stream_visits
            WHERE username = ?
            ORDER BY stream_date DESC
            LIMIT 10
        `);
    }
}
```

**Рекомендация**: Создать отдельный файл `src/backend/database/queries/streak.js` по аналогии с другими queries.

### 3. Логика расчета streak может быть неправильной

**Проблема**: Текущая логика проверяет последовательные календарные дни, но не учитывает, что стримы могут быть не каждый день.

**Файл**: `src/backend/services/bonuses/StreakService.js:111-123, 158-170`

**Пример проблемы**:
- День 1: стрим → посещение
- День 2: нет стрима
- День 3: стрим → посещение
- Streak должен быть 1, но текущая логика может посчитать иначе

**Решение**: Уточнить требования:
- Streak = последовательные дни с посещениями (текущая логика)
- ИЛИ Streak = последовательные стримы с посещениями (нужна другая логика)

### 4. Неправильное использование async/await

**Проблема**: Методы помечены как `async`, но не используют `await` для синхронных операций.

**Файл**: `src/backend/services/bonuses/StreakService.js:85, 132, 179`

```javascript
// Плохо
async getCurrentStreak(username) {
    const getStreak = db.prepare(`...`); // Синхронная операция
    const visits = getStreak.all(...); // Синхронная операция
    // ... синхронный код
    return streak; // Возвращает синхронное значение
}

// Хорошо
getCurrentStreak(username) { // Убрать async
    const visits = this.getVisits.all(...);
    return this._calculateStreakFromVisits(visits, new Date());
}
```

## 🟡 Средние проблемы

### 5. Нарушение Single Responsibility Principle (SOLID)

**Проблема**: `StreakService` делает слишком много:
- Отслеживает состояние стрима
- Отмечает посещения
- Рассчитывает streak
- Проверяет условия для бонусов

**Решение**: Разделить на:
- `StreakService` - только расчет и получение streak
- `StreakVisitTracker` - отслеживание посещений
- Логику проверки бонусов оставить в `StreakBonusHandler`

### 6. Недостаточная валидация входных данных

**Проблема**: В `getStreakForStream` проверяется только наличие `streamDate`, но не его формат.

**Файл**: `src/backend/services/bonuses/StreakService.js:132`

```javascript
// Плохо
async getStreakForStream(username, streamDate) {
    if (!username || typeof username !== 'string' || !streamDate) {
        return 0;
    }
    // streamDate может быть любой строкой
}

// Хорошо
async getStreakForStream(username, streamDate) {
    if (!this._isValidDate(streamDate)) {
        logger.warning('[STREAK] Некорректная дата стрима:', streamDate);
        return 0;
    }
}

_isValidDate(dateString) {
    const date = new Date(dateString);
    return date instanceof Date && !isNaN(date.getTime());
}
```

### 7. Отсутствие обработки edge cases

**Проблема**: Не обрабатываются случаи:
- Стрим начался в один день, закончился в другой
- Часовые пояса
- Переход через полночь

**Решение**: Добавить обработку временных зон и уточнить логику определения даты стрима.

### 8. Магические числа

**Проблема**: В коде используется `LIMIT 10` без объяснения.

**Файл**: `src/backend/services/bonuses/StreakService.js:98, 145`

**Решение**: Вынести в константу:

```javascript
const MAX_STREAK_CHECK_DAYS = 10;
```

### 9. Неоптимальная логика проверки бонуса

**Проблема**: В `StreakBonusHandler` проверка `streak % STREAK_BONUS.MIN_STREAK === 0` означает, что бонус начисляется только при 3, 6, 9, 12... но не при 4, 5, 7, 8...

**Файл**: `src/backend/services/bonuses/StreakBonusHandler.js:27`

**Вопрос**: Это намеренное поведение? Если да, нужно добавить комментарий. Если нет - исправить логику.

**Вариант 1** (текущий): Бонус только при кратных 3
```javascript
if (streak >= STREAK_BONUS.MIN_STREAK && streak % STREAK_BONUS.MIN_STREAK === 0)
```

**Вариант 2**: Бонус при достижении каждого нового уровня
```javascript
const previousStreak = await this._getPreviousStreak(username);
if (streak >= STREAK_BONUS.MIN_STREAK && streak > previousStreak && streak % STREAK_BONUS.MIN_STREAK === 0)
```

## 🟢 Мелкие улучшения

### 10. Улучшение логирования

**Проблема**: Недостаточно контекста в логах ошибок.

**Решение**: Добавить больше контекста:
```javascript
logger.error('[STREAK] Ошибка при отметке посещения:', {
    error: error.message,
    username: normalizedUsername,
    streamDate: this.currentStreamDate,
    stack: error.stack
});
```

### 11. Типизация (для TypeScript)

**Проблема**: Нет типов для возвращаемых значений.

**Решение**: Добавить JSDoc комментарии или мигрировать на TypeScript.

### 12. Тестируемость

**Проблема**: Сложно тестировать из-за зависимостей от `eventBus`, `db`, `streamSession`.

**Решение**: Использовать dependency injection:
```javascript
constructor(dependencies = {}) {
    this.db = dependencies.db || db;
    this.eventBus = dependencies.eventBus || eventBus;
    this.streamSession = dependencies.streamSession || getStreamSessionService();
}
```

### 13. Консистентность с другими сервисами

**Проблема**: `StreakService` использует прямые вызовы `db.prepare()`, в то время как другие сервисы используют queries из `queries/`.

**Решение**: Создать `src/backend/database/queries/streak.js` и использовать prepared statements оттуда.

## 📋 План рефакторинга

### Приоритет 1 (Критично) - ✅ ВЫПОЛНЕНО
1. ✅ Вынести prepared statements в отдельный файл queries (`queries/streak.js`)
2. ✅ Убрать дублирование логики расчета streak (метод `_calculateStreakFromVisits`)
3. ✅ Исправить использование async/await (убраны ненужные async)
4. ⚠️ Уточнить логику расчета streak (календарные дни vs стримы) - требует уточнения требований

### Приоритет 2 (Важно)
5. ✅ Разделить ответственности (SRP)
6. ✅ Добавить валидацию входных данных
7. ✅ Исправить логику начисления бонусов (если нужно)
8. ✅ Вынести магические числа в константы

### Приоритет 3 (Желательно)
9. ✅ Улучшить обработку ошибок
10. ✅ Добавить JSDoc комментарии
11. ✅ Улучшить тестируемость через DI

## 🔍 Дополнительные замечания

### Потенциальные баги

1. **Race condition**: Если два сообщения придут одновременно от одного пользователя, оба могут попытаться отметить посещение. Защита есть через `visitedUsers` Set, но это только для текущего стрима.

2. **Проблема с датами**: Если стрим начался в 23:00 и закончился в 01:00 следующего дня, какая дата считается? Текущая логика использует `startedAt`, что может быть неправильно.

3. **Память**: `visitedUsers` Set может расти, если стрим длинный. Нужен лимит или очистка старых записей.

### Вопросы для уточнения

1. Streak считается по календарным дням или по стримам?
2. Что если стрим длится несколько дней?
3. Нужно ли начислять бонус только один раз за уровень streak или каждый раз при достижении?
4. Что делать, если пользователь был на стриме, но не написал сообщение? (текущая логика не учитывает)

## ✅ Что сделано хорошо

1. ✅ Использование prepared statements (теперь оптимизировано)
2. ✅ Обработка ошибок с try-catch (улучшена)
3. ✅ Логирование важных событий (расширено)
4. ✅ Нормализация username (toLowerCase) - вынесено в метод
5. ✅ Защита от дублирования посещений через Set
6. ✅ Использование UNIQUE constraint в БД
7. ✅ Индексы для оптимизации запросов
8. ✅ JSDoc документация добавлена
9. ✅ Валидация входных данных улучшена
10. ✅ Консистентность с другими сервисами (queries вынесены)

## 📊 Результаты рефакторинга

### Выполнено (Приоритет 1)
- ✅ Вынесены prepared statements в `queries/streak.js`
- ✅ Устранено дублирование логики расчета streak
- ✅ Исправлено использование async/await
- ✅ Добавлена валидация входных данных
- ✅ Улучшена обработка ошибок
- ✅ Добавлена JSDoc документация
- ✅ Вынесены магические числа в константы

### Требует уточнения
- ⚠️ Логика расчета streak (календарные дни vs стримы)
- ⚠️ Логика начисления бонусов (кратные 3 vs каждый уровень)

### Рекомендуется (Приоритет 2-3)
- 📝 Разделение ответственностей (SRP)
- 📝 Dependency Injection для тестируемости
- 📝 Обработка временных зон
